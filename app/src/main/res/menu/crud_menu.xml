<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_agregar"
        android:icon="@drawable/ic_add"
        android:title="@string/registrar_nuevo_producto_lbl"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_guardar"
        android:icon="@drawable/ic_guardar"
        android:title="@string/guardar_menu_label"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/menu_eliminar"
        android:icon="@drawable/ic_delete"
        android:title="@string/eliminar_menu_label"
        app:showAsAction="always"
        />



</menu>